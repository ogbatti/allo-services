# Local development / Développement local
DATABASE_URL=postgresql://allo:allo@localhost:5434/allo_services?schema=public
PORT=3001
NEXT_PUBLIC_API_BASE_URL=http://localhost:3001/api/v1
AUTH_SECRET=change-me-in-production

# Default connectors when tenant.connectors is omitted
# (tenant config wins). Values: simulator | stub-momo / stub-sms
PAYMENT_CONNECTOR=simulator
SMS_CONNECTOR=simulator
