/**
 * Seed is mostly handled at runtime by TenantsService (config/tenants/*.json).
 * This script is a no-op placeholder for future demo fixtures.
 */
console.log("Allô Services seed: tenants/journeys load from config/ on API boot.");
